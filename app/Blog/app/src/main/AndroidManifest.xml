<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.lqcnb.blog">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".avtivity.MyArticlesActivity"></activity>
        <activity android:name=".avtivity.EditMemberActivity" />
        <activity android:name=".avtivity.SetActivity" />
        <activity android:name=".avtivity.MemberInfoActivity" />
        <activity android:name=".avtivity.UpdatePasswordActivity" />
        <activity android:name=".avtivity.RegeditActivity" />
        <activity android:name=".avtivity.LoginActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".avtivity.SearchActivity" />
        <activity android:name=".avtivity.DetailActivity" />
        <activity android:name=".avtivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>